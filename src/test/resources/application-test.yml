spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///iterview
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

auth:
  token:
    secret: test-only-secret
    ttl-seconds: 86400
